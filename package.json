{
  "name": "pop2048-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 8787",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 8788",
    "check": "node scripts/smoke-test.mjs && npm run build"
  },
  "dependencies": {
    "@filoz/synapse-sdk": "^1.0.1",
    "viem": "^2.54.1"
  },
  "overrides": {
    "ws": "8.21.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
